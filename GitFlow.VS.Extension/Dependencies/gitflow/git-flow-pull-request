# $Id$
# vim:et:ft=sh:sts=2:sw=2

gitflow_create_pull_request()
{
    TARGETBRANCH=$1
	BRANCH=$2
    TITLE=$3

    az login --identity
    #Insert title input
    az repos pr create --source-branch $BRANCH --target-branch $TARGETBRANCH --title "$TITLE"
}
